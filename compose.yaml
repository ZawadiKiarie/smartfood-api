services:
  #Backend API
  smartfood-api:
    container_name: smartfood_backend
    build: ./
    command: npm start
    working_dir: /usr/src/smartfood-api
    environment:
      POSTGRES_URI: postgres://postgres:zawadi2005@postgres:5432/smartfood-docker
      REDIS_URI: redis://redis:6379
    depends_on:
      - postgres
      - redis
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/smartfood-api

  #Postgres
  postgres:
    build: ./postgres
    ports:
      - "5436:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: zawadi2005
      POSTGRES_DB: smartfood-docker

  #Redis
  redis:
    image: "redis"
    ports:
      - "6379:6379"
